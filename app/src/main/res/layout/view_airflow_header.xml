<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_marginTop="40dp"
    android:layout_height="wrap_content">


    <com.lihang.ShadowLayout
        android:id="@+id/headerSL"
        android:layout_width="100dp"
        android:layout_height="60dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:hl_cornerRadius="16dp"
        app:hl_layoutBackground="@color/cl_button"
        app:hl_layoutBackground_true="@color/cl_indicator"
        app:hl_shadowColor="@color/cl_000000"
        app:hl_shadowLimit="5dp"
        app:hl_shapeMode="selected">

        <TextView
            android:id="@+id/autoTV"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/auto"
            android:textColor="@color/cl_ffffff"
            android:textSize="20sp" />

    </com.lihang.ShadowLayout>

</androidx.constraintlayout.widget.ConstraintLayout>

